package com.example.project.auth;

/**
 * Authentication & Authorization feature module.
 *
 * This package will contain:
 * - controller/  — Login, register, refresh token endpoints
 * - service/     — Authentication logic, JWT generation
 * - dto/         — Login/Register request & response DTOs
 */
