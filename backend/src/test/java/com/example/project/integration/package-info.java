package com.example.project.integration;

/**
 * Integration tests package.
 *
 * Place integration tests here — tests that require Spring context
 * and/or external services (database, Redis, etc.).
 *
 * Use @Tag("integration") on test classes for selective execution.
 * Consider using @Testcontainers for database integration tests.
 */
