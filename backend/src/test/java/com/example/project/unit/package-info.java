package com.example.project.unit;

/**
 * Unit tests package.
 *
 * Place unit tests here — tests that do NOT require Spring context
 * or external dependencies (database, network, etc.).
 *
 * Use @Tag("unit") on test classes for selective execution.
 */
